# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@726d6c3bc206f053b843036ed863408067188994

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@726d6c3bc206f053b843036ed863408067188994
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
