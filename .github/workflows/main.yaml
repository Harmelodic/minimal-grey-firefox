# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@dd3c524b3f9f0f21d3c19962ba52ba58d835bfda

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@dd3c524b3f9f0f21d3c19962ba52ba58d835bfda
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
