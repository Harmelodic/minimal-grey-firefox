# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@ec7c612da71abf16c3e9ce28fe239b9e0089d616

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@ec7c612da71abf16c3e9ce28fe239b9e0089d616
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
