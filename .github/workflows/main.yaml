# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@bbc40bdd16e74d3d44ca7032d6d855d1909af9d1

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@bbc40bdd16e74d3d44ca7032d6d855d1909af9d1
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
