# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@dbf241b1ce64ae49b3393f9560e9e6a4c524fd1d

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@dbf241b1ce64ae49b3393f9560e9e6a4c524fd1d
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
