# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@a489a10f97c90beecf030674b2cdd016d9f168ed

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@a489a10f97c90beecf030674b2cdd016d9f168ed
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
