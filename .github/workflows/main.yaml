# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@14f9065ffd52508eb634b2cdbc4155a74a7873f6

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@14f9065ffd52508eb634b2cdbc4155a74a7873f6
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
