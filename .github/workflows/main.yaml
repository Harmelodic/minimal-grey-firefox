# $schema: http://json.schemastore.org/github-workflow.json
name: main

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@a3fbc4cc16ed5ad102d2defa263555327a2b17b3

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@a3fbc4cc16ed5ad102d2defa263555327a2b17b3
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
