# $schema: http://json.schemastore.org/github-workflow.json
name: tag

on:
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  release-browser-extension:
    uses: harmelodic/workflows/.github/workflows/release-browser-extension.yaml@f4e30f59f915e57676015f1c829f3a0ffff03c72
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.ref_name }}
