# $schema: http://json.schemastore.org/github-workflow.json
name: tag

on:
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  release-browser-extension:
    uses: harmelodic/workflows/.github/workflows/release-browser-extension.yaml@55963fb4f50ac267f55c26b8120f7926c773394b
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.ref_name }}
