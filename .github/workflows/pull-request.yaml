# $schema: http://json.schemastore.org/github-workflow.json
name: pull-request

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@613ac589c4da38cc96bf4a5341e972c9e08809db

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@613ac589c4da38cc96bf4a5341e972c9e08809db
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
