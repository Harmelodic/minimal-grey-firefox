# $schema: http://json.schemastore.org/github-workflow.json
name: pull-request

on:
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  verify-github-workflows:
    uses: harmelodic/workflows/.github/workflows/verify-github-workflows.yaml@3b5bbed7dd6943873cd7adf66b8a22c11d134dbd

  build-browser-extension:
    uses: harmelodic/workflows/.github/workflows/build-browser-extension.yaml@3b5bbed7dd6943873cd7adf66b8a22c11d134dbd
    with:
      extension-name: minimal-grey-firefox
      release-version: ${{ github.sha }}
